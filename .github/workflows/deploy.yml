name: Deploy to ai-node

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy locally
        run: |
          cd /home/manager/ai-media
          git pull
          docker compose up -d --build
